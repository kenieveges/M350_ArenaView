# requirements.txt

# Arena API for camera control
arena_api==1.0.0  # Replace with the actual version you are using

# Watchdog for file monitoring
watchdog==2.1.9

# Toml library for configuration file parsing
toml==0.10.2

# Logging library (optional, if using custom logging setup)
logging==0.5.1.2

# Other dependencies (add any additional dependencies you might have)